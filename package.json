{
  "name": "quick-tab-switch-extension",
  "version": "1.5.0",
  "description": "⚡ Instantly switch between your two most recent tabs with Alt+Q. Like Alt+Tab for windows, but for Chrome tabs!",
  "type": "module",
  "scripts": {
    "lint:fix": "eslint *.js --ext .js --fix",
    "package": "powershell -Command \"if (Test-Path 'quick-tab-switch-extension.zip') { Remove-Item -Path 'quick-tab-switch-extension.zip' -Force }\" && powershell -Command \"Compress-Archive -Path 'manifest.json','background.js','icon.png','_locales','store-listings' -DestinationPath 'quick-tab-switch-extension.zip' -Force\""
  },
  "devDependencies": {
    "@eslint/js": "^9.33.0",
    "eslint": "^9.33.0",
    "@types/chrome": "^0.1.4"
  }
}
