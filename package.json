{
  "name": "quick-tab-switch-extension",
  "version": "1.5.0",
  "description": "⚡ Instantly switch between your two most recent tabs with Alt+Q. Like Alt+Tab for windows, but for Chrome tabs!",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "lint:fix": "eslint src/ --ext .ts --fix",
    "package": "npm run build && rm -f chrome-extension.zip && zip -r chrome-extension.zip manifest.json build icon.png _locales"
  },
  "devDependencies": {
    "@eslint/js": "^9.33.0",
    "@types/chrome": "^0.1.4",
    "@typescript-eslint/eslint-plugin": "^8.15.0",
    "@typescript-eslint/parser": "^8.15.0",
    "eslint": "^9.33.0",
    "vite": "^7.1.3"
  }
}
